
/**
 * @swagger
 * /device/user:
 *   get:
 *     summary: Get current user's device
 *     tags: [Device]
 *     security:
 *       - bearerAuth: []
 *     responses:
 *       200: { description: Device info }
 *       401: { description: Unauthorized }
 */
export { };
