
/**
 * @swagger
 * /attendance/today:
 *   get:
 *     summary: Get today's attendance with live timer
 *     tags: [Attendance]
 *     security:
 *       - bearerAuth: []
 *     responses:
 *       200: { description: Today's data with working hours }
 */

export { };
